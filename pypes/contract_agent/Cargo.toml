[package]
name = "contract_agent"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = "1.0.100"
clap = { version = "4.5.54", features = ["derive"] }
pypes_analyser = { version = "0.1.0", path = "../pypes_analyser" }
toml = "0.9.11"
